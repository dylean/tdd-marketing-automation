name: Deploy

on:
  push:
    tags:
      - 'v*'  # 打 tag 时触发，例如: v1.0.0

  workflow_dispatch:  # 手动触发

permissions:
  contents: read
  packages: write  # 如果使用 GitHub Container Registry

jobs:
  deploy:
    name: 构建并推送 Docker 镜像
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: 构建应用
        run: ./gradlew bootJar --no-daemon

      - name: 获取版本号
        id: version
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
          else
            VERSION=$(date +%Y%m%d-%H%M%S)-${GITHUB_SHA::7}
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 版本: $VERSION"

      - name: 登录 Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/tdd-marketing-automation:${{ steps.version.outputs.version }}
            ${{ secrets.DOCKER_HUB_USERNAME }}/tdd-marketing-automation:latest

      - name: 部署成功通知
        if: success()
        run: |
          echo "✅ Docker 镜像已推送到 Docker Hub"
          echo "📦 镜像: ${{ secrets.DOCKER_HUB_USERNAME }}/tdd-marketing-automation:${{ steps.version.outputs.version }}"
          echo ""
          echo "🚀 使用以下命令拉取并运行:"
          echo "docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/tdd-marketing-automation:latest"
          echo "docker-compose up -d"
