/**
 * 领域层 (Domain Layer)
 * 
 * 职责：
 * - 领域模型（Entity, Value Object, Aggregate）
 * - 领域服务（Domain Service）
 * - 仓储接口（Repository Interface）
 * - 领域事件（Domain Event）
 * 
 * 依赖规则：
 * - 不依赖任何其他层
 * - 是整个系统的核心
 * - 保持纯净，不包含技术细节
 */
package com.tdd.ma.domain;
